describe("Dashboard", () => {

  beforeEach(() => {

  });

  it.skip('matches the snapshot', () => {
    const wrapper = shallow(<App />);
    expect(wrapper).toMatchSnapshot();
  });

  it.skip('displays an inventory checklist', () => {
  });

  it.skip('', () => {
  });

});